"""数据对象目录。

POJO 是 Plain Old Python Object 的意思，在本项目中用于放接口和业务传输对象。

职责：
    1. 定义请求参数对象。
    2. 定义响应结果对象。
    3. 定义 service、rag、db 之间传递的数据结构。

建议文件：
    - chat.py：聊天请求、聊天响应、引用来源对象。
    - knowledge.py：知识库文件、解析状态、入库结果对象。
    - user.py：用户、角色、权限对象。

注意：
    1. 字段名要稳定，前后端对接后不要随意改。
    2. 每个对象都要写清楚用途和关键字段含义。
"""
